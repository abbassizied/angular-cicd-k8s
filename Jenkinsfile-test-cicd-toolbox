pipeline {
    agent {
		docker { image 'ziedab/jenkins-cicd-toolbox' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B'
				// sh 'node --version'
            }
        }
    }
}