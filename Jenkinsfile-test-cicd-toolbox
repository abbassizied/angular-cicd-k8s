pipeline {
    agent {
		docker { image 'ziedab/jenkins-cicd-toolbox' }
    }
    stages {
        stage('test k8s commands') {
            steps {
                sh 'kubectl cluster-info'
				// sh 'node --version'
            }
        }
    }
}