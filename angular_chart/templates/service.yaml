apiVersion: v1
kind: Service
metadata:
  name: {{ include "angular_chart.fullname" . }}
  labels:
    {{- include "angular_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "angular_chart.selectorLabels" . | nindent 4 }}
